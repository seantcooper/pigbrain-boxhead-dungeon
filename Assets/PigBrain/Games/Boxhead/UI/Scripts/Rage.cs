using UnityEngine;

namespace pigbrain.game.Boxhead.UI
{
    public class Rage : MonoBehaviour, ISetValue
    {
        // internal float pulse { get => _pulse; set => _pulse = value; }
        // float _pulse = 0;
    }
}